# Configuration d'une instance OpenPulse

Référence exhaustive des variables d'environnement. Elle est **dérivée du
registre** `scripts/env-registry.mjs`, qui est la source unique : ce document
ne peut donc pas diverger du contrôle exécuté au démarrage.

Vérifier une configuration :

```bash
node scripts/check-env.mjs --profil minimal   # le strict nécessaire pour démarrer
node scripts/check-env.mjs --profil complet   # toutes les fonctionnalités
```

## Lecture des tableaux

| Colonne    | Sens                                                                                                                                                             |
| ---------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Niveau     | `requis` : sans elle, rien ne démarre. `conditionnel` : requise si la fonctionnalité qu'elle ouvre est activée. `optionnel` : une valeur par défaut sûre existe. |
| Si absente | Ce qui se produit concrètement.                                                                                                                                  |
| Débloque   | Ce que la variable rend possible.                                                                                                                                |

## Vue d'ensemble

174 variables au total.

| Profil  | Variables |
| ------- | --------- |
| minimal | 43        |
| complet | 76        |

| Niveau       | Variables |
| ------------ | --------- |
| requis       | 55        |
| conditionnel | 24        |
| optionnel    | 95        |

## Dégradations silencieuses

Ces variables ne font échouer personne quand elles manquent : la fonctionnalité disparaît sans bruit. Ce sont celles qu'il faut vérifier en premier.

| Variable                          | Débloque                                                                                                                                                                                                                                                                          |
| --------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `VITE_SUPABASE_URL`               | la liaison du navigateur a la passerelle API ; l'amont repliait sur un domaine de l'editeur, ce qui faisait pointer une instance mal configuree vers un tiers                                                                                                                     |
| `VITE_MARQUE_NOM_PRODUIT`         | le nom affiche dans l'onglet du navigateur et dans l'apercu d'un lien partage ; absente, son marqueur reste litteral dans le HTML                                                                                                                                                 |
| `VITE_CSP_IMG_EXTRA`              | les origines d'images autorisees ; absente, son marqueur reste litteral dans le HTML et la politique devient invalide                                                                                                                                                             |
| `VITE_CSP_CONNECT_EXTRA`          | les origines joignables par requete et par WebSocket ; absente, le navigateur refuse chaque appel de l'application vers son propre backend                                                                                                                                        |
| `VITE_CSP_FRAME_EXTRA`            | les origines integrables en cadre ; meme consequence si elle est absente                                                                                                                                                                                                          |
| `VITE_CSP_MEDIA_EXTRA`            | les origines de flux audio et video ; meme consequence si elle est absente                                                                                                                                                                                                        |
| `VITE_EMAIL_AZURE_API_URL`        | la supervision et la synchronisation de messagerie externalisees ; absente alors que le backend est externe, le panneau affiche 'non configure' et la synchronisation ne demarre jamais                                                                                           |
| `VITE_MEETINGS_API_BASE_URL`      | les appels au service Reunions ; absente alors qu'un des deux backends visio est externe, les sondes sont desactivees sans aucun message                                                                                                                                          |
| `VITE_SENTRY_DSN`                 | la collecte des erreurs de production ; absente, la collecte est desactivee sans aucun avertissement et aucune erreur ne remonte jamais                                                                                                                                           |
| `VITE_OTEL_ENDPOINT`              | le tracage distribue ; absent, le tracage est desactive sans avertissement                                                                                                                                                                                                        |
| `VITE_PLAUSIBLE_API_HOST`         | le point de collecte auto-heberge ; absent alors que le domaine est defini, la bibliotheque emet vers son hote public par defaut, c'est-a-dire hors de votre infrastructure                                                                                                       |
| `VITE_MATOMO_SITE_ID`             | la mesure d'audience auto-hebergee ; la seule presence de cette variable active le traceur                                                                                                                                                                                        |
| `VITE_MATOMO_TRACKER_URL`         | la destination du traceur d'audience ; absente alors que l'identifiant de site est defini, le traceur est actif avec une URL vide : aucune mesure collectee, aucune erreur                                                                                                        |
| `VITE_OPENPULSE_WEB_URL`          | l'ouverture des modules web depuis l'application de bureau ; l'amont repliait sur un domaine de l'editeur, ce qui envoyait les utilisateurs de bureau chez un tiers                                                                                                               |
| `OPENPULSE_INSTANCE`              | la separation de plusieurs instances sur une meme machine : ce nom prefixe les conteneurs, reseaux et volumes Docker. Deux instances qui le partagent partagent aussi leur base — la seconde installation reprendrait la premiere au lieu d'en creer une nouvelle, sans rien dire |
| `DATABASE_URL`                    | la persistance des quatre services HTTP ; VIDE, chacun demarre avec un depot EN MEMOIRE : aucune erreur et toutes les donnees disparaissent au redemarrage                                                                                                                        |
| `VERIFY_JWT`                      | la verification du jeton par la passerelle des fonctions ; l'amont fixait 'false' en dur, rendant chaque fonction de bord joignable sans jeton                                                                                                                                    |
| `OPENPULSE_ORIGINES_AUTORISEES`   | les origines autorisees a appeler les fonctions de bord ; absente, seules les origines de developpement passent et votre frontend est refuse                                                                                                                                      |
| `CORS_ALLOWED_ORIGINS`            | l'autorisation du navigateur a appeler les fonctions ; absente, le code repliait sur une liste d'origines codees en dur appartenant a l'editeur et votre frontend etait refuse, avec pour seule trace la console                                                                  |
| `PUBLIC_APP_URL`                  | les liens envoyes par courriel par 4 fonctions (enquetes, relances, transferts) ; absente, l'amont fabriquait des liens vers un domaine de l'editeur                                                                                                                              |
| `APP_URL`                         | les liens des notifications de partage de document ; second nom du precedent, meme valeur, meme fuite si absent                                                                                                                                                                   |
| `INTERNAL_FUNCTION_SECRET`        | l'authentification des appels internes entre 23 fonctions ; absent, ce chemin n'authentifie jamais et les declencheurs recoivent 401 sans trace autre qu'un journal                                                                                                               |
| `CRON_SECRET`                     | les 11 taches planifiees ; absent, la comparaison a temps constant compare deux chaines vides, renvoie faux, et chaque tache repond 401 : rappels, relances et synchronisations ne partent jamais                                                                                 |
| `INTERNAL_INVOCATION_SECRET`      | le second nom du secret de declenchement interne, utilise par 2 fonctions planifiees ; meme consequence silencieuse                                                                                                                                                               |
| `EMAIL_ENCRYPTION_KEY`            | le chiffrement des identifiants de messagerie de 14 fonctions ET le sel de pseudonymisation du module de conformite, ou le repli litteral 'salt' rend les pseudonymes reproductibles par quiconque lit le code                                                                    |
| `AZURE_GPT5_MINI_ENDPOINT`        | le premier repli economique de la chaine ; absent, la chaine passe au repli suivant, plus couteux par appel, sans aucun avertissement                                                                                                                                             |
| `AZURE_GPT5_MINI_API_KEY`         | le premier repli economique                                                                                                                                                                                                                                                       |
| `AZURE_EMBEDDING_ENDPOINT`        | la vectorisation des documents ; absent, l'indexation ne produit aucun vecteur et la recherche semantique ne renvoie jamais de resultat, sans erreur                                                                                                                              |
| `RESEND_API_KEY`                  | l'envoi reel de courriel par 11 fonctions ; absente, elles ne echouent PAS : elles journalisent 'envoi ignore' et renvoient success:true, l'interface confirmant un envoi qui n'a jamais eu lieu                                                                                  |
| `VAPID_SUBJECT`                   | le contact VAPID ET le sel du hachage des adresses IP dans la fonction de telechargement de transfert, ou le repli litteral 'salt' rend le hachage reversible                                                                                                                     |
| `NEXTCLOUD_BASE_FOLDER`           | le dossier racine des documents ; absent, la racine du compte est utilisee et les documents applicatifs se melangent aux fichiers personnels                                                                                                                                      |
| `DOCUSEAL_WEBHOOK_SECRET`         | la verification des rappels du service de signature ; absent, un rappel non signe peut declarer un document signe                                                                                                                                                                 |
| `QONTO_ENCRYPTION_KEY`            | le chiffrement des jetons bancaires en base ; absente, ils sont conserves en clair                                                                                                                                                                                                |
| `QONTO_WEBHOOK_SECRET`            | la verification des rappels bancaires                                                                                                                                                                                                                                             |
| `GOOGLE_TOKEN_ENCRYPTION_KEY`     | le chiffrement dedie des jetons externes ; absente, le code repliait sur la cle de role de service comme matiere de chiffrement, pour le dechiffrement seulement, et tout nouveau chiffrement echoue                                                                              |
| `OAUTH_STATE_SECRET`              | la signature de l'etat OAuth ; absent, l'echange est expose au rejeu d'etat                                                                                                                                                                                                       |
| `SLACK_WEBHOOK_URL`               | le canal de messagerie d'equipe de l'assistant ; absent, le canal est ignore en silence                                                                                                                                                                                           |
| `TEAMS_WEBHOOK_URL`               | le canal collaboratif de l'assistant ; absent, le canal est ignore en silence                                                                                                                                                                                                     |
| `WHATSAPP_TOKEN`                  | le canal de messagerie mobile de l'assistant                                                                                                                                                                                                                                      |
| `WHATSAPP_PHONE_NUMBER_ID`        | le numero emetteur du canal mobile                                                                                                                                                                                                                                                |
| `TWILIO_ACCOUNT_SID`              | le canal SMS et voix                                                                                                                                                                                                                                                              |
| `TWILIO_AUTH_TOKEN`               | le canal SMS et voix                                                                                                                                                                                                                                                              |
| `TWILIO_PHONE_NUMBER`             | le numero emetteur SMS et voix                                                                                                                                                                                                                                                    |
| `PRODUCT_WEBHOOK_SECRET`          | la signature des rappels envoyes au produit partenaire                                                                                                                                                                                                                            |
| `DRIVE_ENV`                       | la validation de configuration du service Drive : elle ne s'execute QUE si la valeur vaut prod. Toute autre valeur laisse le service demarrer sans authentification, sans stockage reel et avec un depot en memoire, sans le moindre avertissement                                |
| `DRIVE_AUTH_MODE`                 | l'authentification du service Drive ; la valeur disabled ouvre l'API a tous et n'est refusee qu'en environnement prod                                                                                                                                                             |
| `DRIVE_CORS_ORIGINS`              | les origines autorisees a appeler le service Drive ; absente, le repli est le seul poste de developpement local et votre frontend est refuse par le navigateur                                                                                                                    |
| `DRIVE_REQUIRE_MFA`               | l'exigence d'une session a second facteur pour ouvrir le Drive ; passer a false retire cette protection sans que rien ne le signale dans l'interface                                                                                                                              |
| `AZURE_STORAGE_CONNECTION_STRING` | le stockage reel des fichiers du Drive et des enregistrements de reunion ; VIDE, les URL signees sont factices : l'envoi semble reussir et aucun octet n'est ecrit                                                                                                                |
| `AZURE_STORAGE_ACCOUNT_KEY`       | la seconde moitie de la variante en deux morceaux du stockage des services ; sans elle, AZURE_STORAGE_ACCOUNT seul ne suffit pas et les URL signees restent factices                                                                                                              |
| `AZURE_STORAGE_ACCOUNT`           | la variante en deux morceaux du stockage des services, a completer par AZURE_STORAGE_ACCOUNT_KEY ; l ancienne variable homonyme de la pile plateforme a ete retiree, elle n avait plus de consommateur                                                                            |
| `EMAIL_ENV`                       | la validation de configuration du service Email : elle ne s'execute QUE si la valeur vaut prod. Toute autre valeur laisse le service demarrer sans authentification, sans stockage reel et avec un depot en memoire, sans le moindre avertissement                                |
| `EMAIL_AUTH_MODE`                 | l'authentification du service Email ; la valeur disabled ouvre l'API a tous et n'est refusee qu'en environnement prod                                                                                                                                                             |
| `EMAIL_CORS_ORIGINS`              | les origines autorisees a appeler le service Email ; absente, le repli est le seul poste de developpement local et votre frontend est refuse par le navigateur                                                                                                                    |
| `MEETINGS_ENV`                    | la validation de configuration du service Meetings : elle ne s'execute QUE si la valeur vaut prod. Toute autre valeur laisse le service demarrer sans authentification, sans stockage reel et avec un depot en memoire, sans le moindre avertissement                             |
| `MEETINGS_AUTH_MODE`              | l'authentification du service Meetings ; la valeur disabled ouvre l'API a tous et n'est refusee qu'en environnement prod                                                                                                                                                          |
| `MEETINGS_CORS_ORIGINS`           | les origines autorisees a appeler le service Meetings ; absente, le repli est le seul poste de developpement local et votre frontend est refuse par le navigateur                                                                                                                 |
| `PULSE_ENV`                       | la validation de configuration du service Pulse : elle ne s'execute QUE si la valeur vaut prod. Toute autre valeur laisse le service demarrer sans authentification, sans stockage reel et avec un depot en memoire, sans le moindre avertissement                                |
| `PULSE_AUTH_MODE`                 | l'authentification du service Pulse ; la valeur disabled ouvre l'API a tous et n'est refusee qu'en environnement prod                                                                                                                                                             |
| `PULSE_CORS_ORIGINS`              | les origines autorisees a appeler le service Pulse ; absente, le repli est le seul poste de developpement local et votre frontend est refuse par le navigateur                                                                                                                    |

## Construction du frontend

| Variable                                   | Niveau       | Défaut       | Si absente     | Débloque                                                                                                                                                                                |
| ------------------------------------------ | ------------ | ------------ | -------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `NODE_ENV`                                 | requis       | `production` | fonctionnalite | le mode de construction : hors 'production', le bundle n'est pas minifie et les bandeaux de diagnostic restent visibles                                                                 |
| `OPENPULSE_BUILD_AT`                       | optionnel    | —            | fonctionnalite | l'horodatage affiche dans la page de diagnostic ; absent, l'heure d'execution du prebuild est utilisee                                                                                  |
| `OPENPULSE_GIT_SHA`                        | requis       | —            | demarrage      | la tracabilite du build ; le prebuild refuse de produire un bundle sans SHA complet lorsque .git est absent de l'image                                                                  |
| `VITE_AUTHENTIK_SSO_ENABLED`               | optionnel    | `false`      | fonctionnalite | le bouton d'authentification unique sur l'ecran de connexion ; absent, seule la connexion par mot de passe est proposee                                                                 |
| `VITE_CSP_CONNECT_EXTRA`                   | requis       | —            | silence        | les origines joignables par requete et par WebSocket ; absente, le navigateur refuse chaque appel de l'application vers son propre backend                                              |
| `VITE_CSP_FRAME_EXTRA`                     | requis       | —            | silence        | les origines integrables en cadre ; meme consequence si elle est absente                                                                                                                |
| `VITE_CSP_IMG_EXTRA`                       | requis       | —            | silence        | les origines d'images autorisees ; absente, son marqueur reste litteral dans le HTML et la politique devient invalide                                                                   |
| `VITE_CSP_MEDIA_EXTRA`                     | requis       | —            | silence        | les origines de flux audio et video ; meme consequence si elle est absente                                                                                                              |
| `VITE_DOCUMENTS_AI_PANEL`                  | optionnel    | `on`         | fonctionnalite | l'affichage du panneau Assistant IA des documents ; absent, le panneau est visible et gere lui-meme son mode non configure                                                              |
| `VITE_DOCUMENTS_BACKEND`                   | optionnel    | `legacy`     | fonctionnalite | le choix du backend Documents ; absent, le mode historique est conserve et le panneau Drive externe reste masque                                                                        |
| `VITE_DOCUMENTS_UPLOAD_BACKEND`            | optionnel    | `auto`       | fonctionnalite | la cible d'envoi des fichiers ; absent, la cible est deduite du backend Documents                                                                                                       |
| `VITE_DRIVE_API_URL`                       | conditionnel | —            | fonctionnalite | les appels au service Drive ; absente alors que le backend Documents est externe, toute action Drive echoue sur 'API indisponible'                                                      |
| `VITE_EMAIL_AZURE_API_URL`                 | conditionnel | —            | silence        | la supervision et la synchronisation de messagerie externalisees ; absente alors que le backend est externe, le panneau affiche 'non configure' et la synchronisation ne demarre jamais |
| `VITE_EMAIL_BACKEND`                       | optionnel    | `supabase`   | fonctionnalite | le backend de messagerie ; absent, la messagerie historique est utilisee sans supervision externe                                                                                       |
| `VITE_INTERNAL_TOOL_EMBED_RUNTIME_ENABLED` | optionnel    | `false`      | fonctionnalite | les outils internes integres en iframe ; absent, les entrees de menu correspondantes sont masquees                                                                                      |
| `VITE_MARQUE_NOM_PRODUIT`                  | requis       | `OpenPulse`  | silence        | le nom affiche dans l'onglet du navigateur et dans l'apercu d'un lien partage ; absente, son marqueur reste litteral dans le HTML                                                       |
| `VITE_MATOMO_SITE_ID`                      | optionnel    | —            | silence        | la mesure d'audience auto-hebergee ; la seule presence de cette variable active le traceur                                                                                              |
| `VITE_MATOMO_TRACKER_URL`                  | conditionnel | —            | silence        | la destination du traceur d'audience ; absente alors que l'identifiant de site est defini, le traceur est actif avec une URL vide : aucune mesure collectee, aucune erreur              |
| `VITE_MEETINGS_API_BASE_URL`               | conditionnel | —            | silence        | les appels au service Reunions ; absente alors qu'un des deux backends visio est externe, les sondes sont desactivees sans aucun message                                                |
| `VITE_OPENPULSE_WEB_URL`                   | conditionnel | —            | silence        | l'ouverture des modules web depuis l'application de bureau ; l'amont repliait sur un domaine de l'editeur, ce qui envoyait les utilisateurs de bureau chez un tiers                     |
| `VITE_OTEL_ENDPOINT`                       | optionnel    | —            | silence        | le tracage distribue ; absent, le tracage est desactive sans avertissement                                                                                                              |
| `VITE_PLAUSIBLE_API_HOST`                  | optionnel    | —            | silence        | le point de collecte auto-heberge ; absent alors que le domaine est defini, la bibliotheque emet vers son hote public par defaut, c'est-a-dire hors de votre infrastructure             |
| `VITE_PLAUSIBLE_DOMAIN`                    | optionnel    | —            | fonctionnalite | la mesure d'audience sans cookie ; absente, la mesure est desactivee                                                                                                                    |
| `VITE_PULSE_AZURE_API_URL`                 | conditionnel | —            | fonctionnalite | les appels au service de discussion ; absente alors que le backend est externe, chaque appel leve 'non configure'                                                                       |
| `VITE_PULSE_AZURE_WS_URL`                  | optionnel    | —            | fonctionnalite | le temps reel de la discussion ; absente, l'URL est derivee de la base API (https vers wss, chemin /api/pulse/ws)                                                                       |
| `VITE_PULSE_BACKEND`                       | optionnel    | `supabase`   | fonctionnalite | le backend de la discussion interne ; absent, le mode historique est conserve                                                                                                           |
| `VITE_SENTRY_DSN`                          | optionnel    | —            | silence        | la collecte des erreurs de production ; absente, la collecte est desactivee sans aucun avertissement et aucune erreur ne remonte jamais                                                 |
| `VITE_SUPABASE_PUBLISHABLE_KEY`            | requis       | —            | demarrage      | l'authentification du navigateur ; absente, une exception est levee au chargement du module client et l'application affiche un ecran blanc                                              |
| `VITE_SUPABASE_URL`                        | requis       | —            | silence        | la liaison du navigateur a la passerelle API ; l'amont repliait sur un domaine de l'editeur, ce qui faisait pointer une instance mal configuree vers un tiers                           |
| `VITE_TRANSCRIPTION_BACKEND`               | optionnel    | `supabase`   | fonctionnalite | le backend du pipeline de transcription ; absent, le mode historique est conserve                                                                                                       |
| `VITE_VISIO_BACKEND`                       | optionnel    | `supabase`   | fonctionnalite | le backend des salles de visioconference ; absent, le mode historique est conserve                                                                                                      |

## Plateforme (base, authentification, stockage, passerelle)

| Variable                   | Niveau       | Défaut             | Si absente     | Débloque                                                                                                                                                                                                                                                                          |
| -------------------------- | ------------ | ------------------ | -------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `ANON_KEY`                 | requis       | —                  | demarrage      | l'acces public a la passerelle ; doit etre identique a VITE_SUPABASE_PUBLISHABLE_KEY                                                                                                                                                                                              |
| `API_EXTERNAL_URL`         | requis       | —                  | demarrage      | l'emetteur des jetons et les liens de confirmation envoyes par courriel                                                                                                                                                                                                           |
| `AUTH_ADMIN_PASSWORD`      | requis       | —                  | demarrage      | la connexion du service d'authentification, qui applique ses propres migrations ; absent, aucune session n'est possible                                                                                                                                                           |
| `AUTH_REDIRECT_ALLOW_LIST` | optionnel    | —                  | fonctionnalite | les redirections autorisees apres authentification, au-dela de SITE_URL ; absente, seule SITE_URL est acceptee et les liens d'invitation vers un autre domaine sont refuses                                                                                                       |
| `AUTHENTICATOR_PASSWORD`   | requis       | —                  | demarrage      | la connexion de l'API REST a la base, avec un role sans privilege d'elevation ; absent, l'API ne demarre pas                                                                                                                                                                      |
| `JWT_SECRET`               | requis       | —                  | demarrage      | la signature de tous les jetons : identite, REST, temps reel, stockage, fonctions ; absent, personne ne peut se connecter                                                                                                                                                         |
| `OPENPULSE_INSTANCE`       | optionnel    | `openpulse`        | silence        | la separation de plusieurs instances sur une meme machine : ce nom prefixe les conteneurs, reseaux et volumes Docker. Deux instances qui le partagent partagent aussi leur base — la seconde installation reprendrait la premiere au lieu d'en creer une nouvelle, sans rien dire |
| `POSTGRES_DB`              | requis       | `openpulse`        | demarrage      | le nom de la base applicative                                                                                                                                                                                                                                                     |
| `POSTGRES_HOST`            | requis       | —                  | demarrage      | la connexion du service temps reel a la base                                                                                                                                                                                                                                      |
| `POSTGRES_PASSWORD`        | requis       | —                  | demarrage      | la connexion du service temps reel ; absent, l'application demarre mais sans aucune mise a jour live                                                                                                                                                                              |
| `POSTGRES_PORT`            | optionnel    | `5432`             | fonctionnalite | le port de la base ; le compose fixe 5432 en dur, cette variable est declarative                                                                                                                                                                                                  |
| `POSTGRES_USER`            | conditionnel | `postgres`         | demarrage      | le compte proprietaire du schema, sur une base PostgreSQL externe                                                                                                                                                                                                                 |
| `POSTGREST_URL`            | requis       | `http://rest:3000` | demarrage      | l'adresse HTTP de l'API REST, utilisee par le service de stockage ; absente, les listes restent vides                                                                                                                                                                             |
| `PUBLIC_API_URL`           | requis       | —                  | demarrage      | l'origine publique de la passerelle ; doit etre egale a VITE_SUPABASE_URL, sinon le navigateur appelle une adresse que la politique de securite refuse                                                                                                                            |
| `PUBLIC_URL`               | requis       | —                  | demarrage      | l'origine publique de l'application, vue par le navigateur ; sert d'origine autorisee a la passerelle et de cible de redirection                                                                                                                                                  |
| `REALTIME_ADMIN_PASSWORD`  | requis       | —                  | demarrage      | la connexion du service temps reel a la base ; absent, les abonnements aux changements restent muets sans erreur visible                                                                                                                                                          |
| `REALTIME_ENC_KEY`         | requis       | —                  | demarrage      | le chiffrement des metadonnees du service temps reel ; exactement 16 octets                                                                                                                                                                                                       |
| `REALTIME_SECRET_KEY_BASE` | requis       | —                  | demarrage      | la derivation des cles de session du service temps reel ; au moins 64 octets                                                                                                                                                                                                      |
| `S3_ACCESS_KEY`            | conditionnel | —                  | fonctionnalite | l'ecriture dans le stockage objet ; la composition auto-hebergee ecrit sur un volume local et n'en a pas besoin                                                                                                                                                                   |
| `S3_SECRET_KEY`            | conditionnel | —                  | fonctionnalite | l'ecriture dans le stockage objet, meme condition que S3_ACCESS_KEY                                                                                                                                                                                                               |
| `SERVICE_ROLE_KEY`         | requis       | —                  | demarrage      | les acces privilegies des fonctions de bord ; ne doit jamais etre expose au navigateur                                                                                                                                                                                            |
| `SITE_URL`                 | requis       | —                  | fonctionnalite | la redirection apres authentification                                                                                                                                                                                                                                             |
| `STORAGE_ADMIN_PASSWORD`   | requis       | —                  | demarrage      | la connexion du service de stockage ; absent, aucun fichier ne peut etre depose ni relu                                                                                                                                                                                           |
| `SUPABASE_JWKS`            | conditionnel | —                  | demarrage      | la verification des jetons signes en ES256 ou RS256 ; absente avec de tels jetons, tous les appels sont refuses                                                                                                                                                                   |
| `VERIFY_JWT`               | requis       | `true`             | silence        | la verification du jeton par la passerelle des fonctions ; l'amont fixait 'false' en dur, rendant chaque fonction de bord joignable sans jeton                                                                                                                                    |

## Fonctions de bord

| Variable                         | Niveau    | Défaut             | Si absente     | Débloque                                                                                                                                                                                                         |
| -------------------------------- | --------- | ------------------ | -------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `APP_URL`                        | requis    | —                  | silence        | les liens des notifications de partage de document ; second nom du precedent, meme valeur, meme fuite si absent                                                                                                  |
| `AZURE_EMBEDDING_ENDPOINT`       | optionnel | —                  | silence        | la vectorisation des documents ; absent, l'indexation ne produit aucun vecteur et la recherche semantique ne renvoie jamais de resultat, sans erreur                                                             |
| `AZURE_GPT5_MINI_API_KEY`        | optionnel | —                  | silence        | le premier repli economique                                                                                                                                                                                      |
| `AZURE_GPT5_MINI_ENDPOINT`       | optionnel | —                  | silence        | le premier repli economique de la chaine ; absent, la chaine passe au repli suivant, plus couteux par appel, sans aucun avertissement                                                                            |
| `AZURE_GPT52_API_KEY`            | optionnel | —                  | fonctionnalite | le deuxieme repli de la chaine                                                                                                                                                                                   |
| `AZURE_GPT52_ENDPOINT`           | optionnel | —                  | fonctionnalite | le deuxieme repli de la chaine de modeles                                                                                                                                                                        |
| `AZURE_IMAGE_API_KEY`            | optionnel | —                  | fonctionnalite | la generation d'images                                                                                                                                                                                           |
| `AZURE_IMAGE_DEPLOYMENT`         | optionnel | —                  | fonctionnalite | le nom du deploiement de generation d'images                                                                                                                                                                     |
| `AZURE_IMAGE_ENDPOINT`           | optionnel | —                  | fonctionnalite | la generation d'images                                                                                                                                                                                           |
| `AZURE_OPENAI_API_KEY`           | optionnel | —                  | fonctionnalite | l'authentification du modele principal, utilise par 52 fonctions                                                                                                                                                 |
| `AZURE_OPENAI_API_KEY_FALLBACK`  | optionnel | —                  | fonctionnalite | le deploiement de secours                                                                                                                                                                                        |
| `AZURE_OPENAI_ENDPOINT`          | optionnel | —                  | fonctionnalite | les 51 fonctions d'IA ; absent avec les deux replis absents, chacune leve 'fournisseur non configure' (erreur visible)                                                                                           |
| `AZURE_OPENAI_ENDPOINT_FALLBACK` | optionnel | —                  | fonctionnalite | le deploiement de secours sonde par la verification de sante                                                                                                                                                     |
| `AZURE_TRANSCRIBE_API_KEY`       | optionnel | —                  | fonctionnalite | la transcription audio                                                                                                                                                                                           |
| `AZURE_TRANSCRIBE_ENDPOINT`      | optionnel | —                  | fonctionnalite | la transcription audio ; absent, le pipeline de comptes rendus s'arrete apres l'envoi du fichier                                                                                                                 |
| `AZURE_TTS_API_KEY`              | optionnel | —                  | fonctionnalite | la cle du service de synthese vocale ; meme effet que l'absence d'endpoint                                                                                                                                       |
| `AZURE_TTS_ENDPOINT`             | optionnel | —                  | fonctionnalite | l'hote du service de synthese vocale ; absente, jarvis-tts refuse poliment et l'assistant reste muet                                                                                                             |
| `BRAVE_SEARCH_API_KEY`           | optionnel | —                  | fonctionnalite | l'outil de recherche web de l'assistant                                                                                                                                                                          |
| `CHORUS_PRO_CLIENT_ID`           | optionnel | —                  | fonctionnalite | la transmission des factures au secteur public                                                                                                                                                                   |
| `CHORUS_PRO_CLIENT_SECRET`       | optionnel | —                  | fonctionnalite | la transmission des factures au secteur public                                                                                                                                                                   |
| `CHORUS_PRO_LOGIN`               | optionnel | —                  | fonctionnalite | le compte de transmission des factures publiques                                                                                                                                                                 |
| `CHORUS_PRO_PASSWORD`            | optionnel | —                  | fonctionnalite | le compte de transmission des factures publiques                                                                                                                                                                 |
| `CORS_ALLOWED_ORIGINS`           | requis    | —                  | silence        | l'autorisation du navigateur a appeler les fonctions ; absente, le code repliait sur une liste d'origines codees en dur appartenant a l'editeur et votre frontend etait refuse, avec pour seule trace la console |
| `CRON_SECRET`                    | requis    | —                  | silence        | les 11 taches planifiees ; absent, la comparaison a temps constant compare deux chaines vides, renvoie faux, et chaque tache repond 401 : rappels, relances et synchronisations ne partent jamais                |
| `DOCUSEAL_API_KEY`               | optionnel | —                  | fonctionnalite | les 4 fonctions de signature electronique                                                                                                                                                                        |
| `DOCUSEAL_WEBHOOK_SECRET`        | optionnel | —                  | silence        | la verification des rappels du service de signature ; absent, un rappel non signe peut declarer un document signe                                                                                                |
| `EMAIL_ENCRYPTION_KEY`           | requis    | —                  | silence        | le chiffrement des identifiants de messagerie de 14 fonctions ET le sel de pseudonymisation du module de conformite, ou le repli litteral 'salt' rend les pseudonymes reproductibles par quiconque lit le code   |
| `EMAIL_TRACKING_HMAC_SECRET`     | optionnel | —                  | fonctionnalite | la signature des liens de suivi de lecture ; absent, la fonction repond 500 et les liens sont morts, ce qui est le comportement sur                                                                              |
| `GOOGLE_CLIENT_ID`               | optionnel | —                  | fonctionnalite | le raccordement de l'agenda et de la visioconference externes                                                                                                                                                    |
| `GOOGLE_CLIENT_SECRET`           | optionnel | —                  | fonctionnalite | le raccordement des services externes                                                                                                                                                                            |
| `GOOGLE_REFRESH_TOKEN`           | optionnel | —                  | fonctionnalite | la creation automatique de liens de reunion par un compte de service                                                                                                                                             |
| `GOOGLE_TOKEN_ENCRYPTION_KEY`    | optionnel | —                  | silence        | le chiffrement dedie des jetons externes ; absente, le code repliait sur la cle de role de service comme matiere de chiffrement, pour le dechiffrement seulement, et tout nouveau chiffrement echoue             |
| `INTERNAL_FUNCTION_SECRET`       | requis    | —                  | silence        | l'authentification des appels internes entre 23 fonctions ; absent, ce chemin n'authentifie jamais et les declencheurs recoivent 401 sans trace autre qu'un journal                                              |
| `INTERNAL_INVOCATION_SECRET`     | requis    | —                  | silence        | le second nom du secret de declenchement interne, utilise par 2 fonctions planifiees ; meme consequence silencieuse                                                                                              |
| `IP_VALIDATOR_SECRET`            | optionnel | —                  | fonctionnalite | la protection de la validation d'adresse IP ; absent avec la liste blanche activee, tous les appels sont refuses                                                                                                 |
| `LINKEDIN_CLIENT_ID`             | optionnel | —                  | fonctionnalite | la publication sur le reseau professionnel                                                                                                                                                                       |
| `LINKEDIN_CLIENT_SECRET`         | optionnel | —                  | fonctionnalite | la publication sur le reseau professionnel                                                                                                                                                                       |
| `META_APP_ID`                    | optionnel | —                  | fonctionnalite | la publication sur les reseaux sociaux grand public                                                                                                                                                              |
| `META_APP_SECRET`                | optionnel | —                  | fonctionnalite | la publication sur les reseaux sociaux grand public                                                                                                                                                              |
| `NEXTCLOUD_APP_PASSWORD`         | optionnel | —                  | fonctionnalite | le mot de passe d'application du compte de service                                                                                                                                                               |
| `NEXTCLOUD_BASE_FOLDER`          | optionnel | —                  | silence        | le dossier racine des documents ; absent, la racine du compte est utilisee et les documents applicatifs se melangent aux fichiers personnels                                                                     |
| `NEXTCLOUD_URL`                  | optionnel | —                  | fonctionnalite | les 4 fonctions de fichiers WebDAV ; absente, l'onglet correspondant reste vide                                                                                                                                  |
| `NEXTCLOUD_USERNAME`             | optionnel | —                  | fonctionnalite | le compte de service du serveur de fichiers                                                                                                                                                                      |
| `OAUTH_STATE_SECRET`             | optionnel | —                  | silence        | la signature de l'etat OAuth ; absent, l'echange est expose au rejeu d'etat                                                                                                                                      |
| `ONLYOFFICE_API_KEY`             | optionnel | —                  | fonctionnalite | la cle de l'espace documentaire                                                                                                                                                                                  |
| `ONLYOFFICE_DOCSPACE_URL`        | optionnel | —                  | fonctionnalite | l'espace documentaire heberge                                                                                                                                                                                    |
| `ONLYOFFICE_DOCUMENT_SERVER_URL` | optionnel | —                  | fonctionnalite | l'edition collaborative de documents                                                                                                                                                                             |
| `ONLYOFFICE_JWT_SECRET`          | optionnel | —                  | fonctionnalite | la confiance entre l'application et le serveur d'edition                                                                                                                                                         |
| `OPENAI_API_KEY`                 | optionnel | —                  | fonctionnalite | la transcription directe par la fonction dediee ; absente, elle repond 500                                                                                                                                       |
| `OPENPULSE_INSTALLATION_CODE`    | requis    | —                  | demarrage      | le code d'installation qui autorise la creation du premier administrateur ; absent, l'instance reste sans administrateur possible                                                                                |
| `OPENPULSE_ORIGINES_AUTORISEES`  | requis    | —                  | silence        | les origines autorisees a appeler les fonctions de bord ; absente, seules les origines de developpement passent et votre frontend est refuse                                                                     |
| `PLATFORM_SSO_JWT_SECRET`        | optionnel | —                  | fonctionnalite | l'emission de jetons d'authentification unique vers un produit partenaire ; absent, la fonction leve 'non configure'                                                                                             |
| `PRODUCT_API_URL`                | optionnel | —                  | fonctionnalite | la cible des provisions de clients vers un produit partenaire                                                                                                                                                    |
| `PRODUCT_WEBHOOK_SECRET`         | optionnel | —                  | silence        | la signature des rappels envoyes au produit partenaire                                                                                                                                                           |
| `PUBLIC_APP_URL`                 | requis    | —                  | silence        | les liens envoyes par courriel par 4 fonctions (enquetes, relances, transferts) ; absente, l'amont fabriquait des liens vers un domaine de l'editeur                                                             |
| `QONTO_API_KEY`                  | optionnel | —                  | fonctionnalite | la synchronisation des operations bancaires ; absente, le module tresorerie reste vide                                                                                                                           |
| `QONTO_CLIENT_ID`                | optionnel | —                  | fonctionnalite | le raccordement bancaire delegue                                                                                                                                                                                 |
| `QONTO_CLIENT_SECRET`            | optionnel | —                  | fonctionnalite | le raccordement bancaire delegue                                                                                                                                                                                 |
| `QONTO_ENCRYPTION_KEY`           | optionnel | —                  | silence        | le chiffrement des jetons bancaires en base ; absente, ils sont conserves en clair                                                                                                                               |
| `QONTO_ORGANIZATION_ID`          | optionnel | —                  | fonctionnalite | l'identification de l'organisation bancaire                                                                                                                                                                      |
| `QONTO_REDIRECT_URI`             | optionnel | —                  | fonctionnalite | le retour d'autorisation bancaire                                                                                                                                                                                |
| `QONTO_WEBHOOK_SECRET`           | optionnel | —                  | silence        | la verification des rappels bancaires                                                                                                                                                                            |
| `RESEND_API_KEY`                 | optionnel | —                  | silence        | l'envoi reel de courriel par 11 fonctions ; absente, elles ne echouent PAS : elles journalisent 'envoi ignore' et renvoient success:true, l'interface confirmant un envoi qui n'a jamais eu lieu                 |
| `SLACK_WEBHOOK_URL`              | optionnel | —                  | silence        | le canal de messagerie d'equipe de l'assistant ; absent, le canal est ignore en silence                                                                                                                          |
| `STALWART_ADMIN_PASSWORD`        | optionnel | —                  | fonctionnalite | l'administration du serveur de messagerie interne, requis seulement si vous montez ce service                                                                                                                    |
| `STALWART_URL`                   | optionnel | —                  | fonctionnalite | la migration et la synchronisation JMAP ; absente, repli sur le nom de service interne, correct dans le compose fourni                                                                                           |
| `SUPABASE_ANON_KEY`              | requis    | —                  | demarrage      | la validation des jetons utilisateur par 73 fonctions ; absente, elles repondent 401 a tout le monde                                                                                                             |
| `SUPABASE_JWT_SECRET`            | optionnel | —                  | fonctionnalite | le repli de signature du point d'entree OAuth de l'application, si vous ne reutilisez pas JWT_SECRET                                                                                                             |
| `SUPABASE_SERVICE_ROLE_KEY`      | requis    | —                  | demarrage      | les acces privilegies de 223 fonctions ; absente, elles echouent des la creation de leur client                                                                                                                  |
| `SUPABASE_URL`                   | requis    | `http://kong:8000` | demarrage      | l'acces des 243 fonctions concernees a la passerelle interne                                                                                                                                                     |
| `TEAMS_WEBHOOK_URL`              | optionnel | —                  | silence        | le canal collaboratif de l'assistant ; absent, le canal est ignore en silence                                                                                                                                    |
| `TIKTOK_CLIENT_KEY`              | optionnel | —                  | fonctionnalite | la publication video courte                                                                                                                                                                                      |
| `TIKTOK_CLIENT_SECRET`           | optionnel | —                  | fonctionnalite | la publication video courte                                                                                                                                                                                      |
| `TWILIO_ACCOUNT_SID`             | optionnel | —                  | silence        | le canal SMS et voix                                                                                                                                                                                             |
| `TWILIO_AUTH_TOKEN`              | optionnel | —                  | silence        | le canal SMS et voix                                                                                                                                                                                             |
| `TWILIO_PHONE_NUMBER`            | optionnel | —                  | silence        | le numero emetteur SMS et voix                                                                                                                                                                                   |
| `VAPID_PRIVATE_KEY`              | optionnel | —                  | fonctionnalite | les notifications navigateur                                                                                                                                                                                     |
| `VAPID_PUBLIC_KEY`               | optionnel | —                  | fonctionnalite | les notifications navigateur ; absente, la fonction leve 'cles VAPID non configurees'                                                                                                                            |
| `VAPID_SUBJECT`                  | optionnel | —                  | silence        | le contact VAPID ET le sel du hachage des adresses IP dans la fonction de telechargement de transfert, ou le repli litteral 'salt' rend le hachage reversible                                                    |
| `WHATSAPP_PHONE_NUMBER_ID`       | optionnel | —                  | silence        | le numero emetteur du canal mobile                                                                                                                                                                               |
| `WHATSAPP_TOKEN`                 | optionnel | —                  | silence        | le canal de messagerie mobile de l'assistant                                                                                                                                                                     |

## Services annexes

| Variable                             | Niveau       | Défaut                          | Si absente     | Débloque                                                                                                                                                                                                                                              |
| ------------------------------------ | ------------ | ------------------------------- | -------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `AZURE_STORAGE_ACCOUNT`              | conditionnel | —                               | silence        | la variante en deux morceaux du stockage des services, a completer par AZURE_STORAGE_ACCOUNT_KEY ; l ancienne variable homonyme de la pile plateforme a ete retiree, elle n avait plus de consommateur                                                |
| `AZURE_STORAGE_ACCOUNT_KEY`          | conditionnel | —                               | silence        | la seconde moitie de la variante en deux morceaux du stockage des services ; sans elle, AZURE_STORAGE_ACCOUNT seul ne suffit pas et les URL signees restent factices                                                                                  |
| `AZURE_STORAGE_CONNECTION_STRING`    | requis       | —                               | silence        | le stockage reel des fichiers du Drive et des enregistrements de reunion ; VIDE, les URL signees sont factices : l'envoi semble reussir et aucun octet n'est ecrit                                                                                    |
| `DATABASE_URL`                       | requis       | —                               | silence        | la persistance des quatre services HTTP ; VIDE, chacun demarre avec un depot EN MEMOIRE : aucune erreur et toutes les donnees disparaissent au redemarrage                                                                                            |
| `DESKTOP_REFRESH_TTL_SECONDS`        | optionnel    | `2592000`                       | demarrage      | la duree de vie du jeton de renouvellement du client de bureau ; hors de l'intervalle 1 a 90 jours la validation refuse de demarrer                                                                                                                   |
| `DRIVE_APP_SECRET`                   | requis       | —                               | demarrage      | la signature des jetons d'envoi de fichier ; la validation refuse de demarrer si la valeur fait moins de 32 caracteres ou vaut la valeur de developpement                                                                                             |
| `DRIVE_AUTH_MODE`                    | requis       | `jwt`                           | silence        | l'authentification du service Drive ; la valeur disabled ouvre l'API a tous et n'est refusee qu'en environnement prod                                                                                                                                 |
| `DRIVE_BLOB_CONTAINER_FILES`         | optionnel    | `openpulse-drive-files`         | fonctionnalite | le conteneur de stockage des fichiers du Drive                                                                                                                                                                                                        |
| `DRIVE_BLOB_CONTAINER_VERSIONS`      | optionnel    | `openpulse-drive-versions`      | fonctionnalite | le conteneur de stockage des versions de fichiers                                                                                                                                                                                                     |
| `DRIVE_CORS_ORIGINS`                 | requis       | —                               | silence        | les origines autorisees a appeler le service Drive ; absente, le repli est le seul poste de developpement local et votre frontend est refuse par le navigateur                                                                                        |
| `DRIVE_ENV`                          | requis       | `prod`                          | silence        | la validation de configuration du service Drive : elle ne s'execute QUE si la valeur vaut prod. Toute autre valeur laisse le service demarrer sans authentification, sans stockage reel et avec un depot en memoire, sans le moindre avertissement    |
| `DRIVE_JWT_AUDIENCE`                 | conditionnel | `openpulse-drive-api`           | demarrage      | l'audience attendue des jetons du service Drive                                                                                                                                                                                                       |
| `DRIVE_JWT_ISSUER`                   | conditionnel | `openpulse`                     | demarrage      | l'emetteur attendu des jetons du service Drive ; exige avec le secret et l'audience en mode jwt                                                                                                                                                       |
| `DRIVE_JWT_SECRET`                   | conditionnel | —                               | demarrage      | la verification des jetons du service Drive ; en mode jwt le service refuse de demarrer sans lui                                                                                                                                                      |
| `DRIVE_JWT_TTL_SECONDS`              | optionnel    | `3600`                          | demarrage      | la duree de vie du jeton d'acces Drive ; hors de l'intervalle 900 a 86400 la validation refuse de demarrer                                                                                                                                            |
| `DRIVE_REQUIRE_MFA`                  | requis       | `true`                          | silence        | l'exigence d'une session a second facteur pour ouvrir le Drive ; passer a false retire cette protection sans que rien ne le signale dans l'interface                                                                                                  |
| `DRIVE_SAS_TTL_MINUTES`              | optionnel    | `15`                            | fonctionnalite | la duree de vie des URL signees d'acces aux fichiers                                                                                                                                                                                                  |
| `EMAIL_AUTH_MODE`                    | requis       | `jwt`                           | silence        | l'authentification du service Email ; la valeur disabled ouvre l'API a tous et n'est refusee qu'en environnement prod                                                                                                                                 |
| `EMAIL_CORS_ORIGINS`                 | requis       | —                               | silence        | les origines autorisees a appeler le service Email ; absente, le repli est le seul poste de developpement local et votre frontend est refuse par le navigateur                                                                                        |
| `EMAIL_ENV`                          | requis       | `prod`                          | silence        | la validation de configuration du service Email : elle ne s'execute QUE si la valeur vaut prod. Toute autre valeur laisse le service demarrer sans authentification, sans stockage reel et avec un depot en memoire, sans le moindre avertissement    |
| `EMAIL_JWT_AUDIENCE`                 | conditionnel | `openpulse-email-api`           | demarrage      | l'audience attendue des jetons du service Email                                                                                                                                                                                                       |
| `EMAIL_JWT_ISSUER`                   | conditionnel | `openpulse`                     | demarrage      | l'emetteur attendu des jetons du service Email ; exige avec le secret et l'audience en mode jwt                                                                                                                                                       |
| `EMAIL_JWT_SECRET`                   | conditionnel | —                               | demarrage      | la verification des jetons du service Email ; en mode jwt le service refuse de demarrer sans lui                                                                                                                                                      |
| `MEETINGS_AUTH_MODE`                 | requis       | `jwt`                           | silence        | l'authentification du service Meetings ; la valeur disabled ouvre l'API a tous et n'est refusee qu'en environnement prod                                                                                                                              |
| `MEETINGS_BLOB_CONTAINER_RECORDINGS` | optionnel    | `openpulse-meetings-recordings` | fonctionnalite | le conteneur de stockage des enregistrements de reunion                                                                                                                                                                                               |
| `MEETINGS_CORS_ORIGINS`              | requis       | —                               | silence        | les origines autorisees a appeler le service Meetings ; absente, le repli est le seul poste de developpement local et votre frontend est refuse par le navigateur                                                                                     |
| `MEETINGS_ENV`                       | requis       | `prod`                          | silence        | la validation de configuration du service Meetings : elle ne s'execute QUE si la valeur vaut prod. Toute autre valeur laisse le service demarrer sans authentification, sans stockage reel et avec un depot en memoire, sans le moindre avertissement |
| `MEETINGS_JWT_AUDIENCE`              | conditionnel | `openpulse-meetings-api`        | demarrage      | l'audience attendue des jetons du service Meetings                                                                                                                                                                                                    |
| `MEETINGS_JWT_ISSUER`                | conditionnel | `openpulse`                     | demarrage      | l'emetteur attendu des jetons du service Meetings ; exige avec le secret et l'audience en mode jwt                                                                                                                                                    |
| `MEETINGS_JWT_SECRET`                | conditionnel | —                               | demarrage      | la verification des jetons du service Meetings ; en mode jwt le service refuse de demarrer sans lui                                                                                                                                                   |
| `MEETINGS_SAS_TTL_MINUTES`           | optionnel    | `30`                            | fonctionnalite | la duree de vie des URL signees d'envoi d'enregistrement                                                                                                                                                                                              |
| `PULSE_AUTH_MODE`                    | requis       | `jwt`                           | silence        | l'authentification du service Pulse ; la valeur disabled ouvre l'API a tous et n'est refusee qu'en environnement prod                                                                                                                                 |
| `PULSE_CORS_ORIGINS`                 | requis       | —                               | silence        | les origines autorisees a appeler le service Pulse ; absente, le repli est le seul poste de developpement local et votre frontend est refuse par le navigateur                                                                                        |
| `PULSE_ENV`                          | requis       | `prod`                          | silence        | la validation de configuration du service Pulse : elle ne s'execute QUE si la valeur vaut prod. Toute autre valeur laisse le service demarrer sans authentification, sans stockage reel et avec un depot en memoire, sans le moindre avertissement    |
| `PULSE_JWT_AUDIENCE`                 | conditionnel | `openpulse-pulse-api`           | demarrage      | l'audience attendue des jetons du service Pulse                                                                                                                                                                                                       |
| `PULSE_JWT_ISSUER`                   | conditionnel | `openpulse`                     | demarrage      | l'emetteur attendu des jetons du service Pulse ; exige avec le secret et l'audience en mode jwt                                                                                                                                                       |
| `PULSE_JWT_SECRET`                   | conditionnel | —                               | demarrage      | la verification des jetons du service Pulse ; en mode jwt le service refuse de demarrer sans lui                                                                                                                                                      |

## Valeurs interdites

Le contrôle refuse certaines valeurs, même syntaxiquement correctes : gabarits laissés en place, secrets d'exemple, origines ouvertes à tous. Voir `REMPLISSAGES_INTERDITS` dans le registre.
