/**
 * @deprecated Use imports from '@/lib/formatters' instead.
 * This file re-exports from formatters for backward compatibility.
 */
export {
  formatPercentage,
  formatCompactNumber,
  formatDateFR,
  formatTime,
  formatDuration,
  formatPhone,
  formatFileSizeFR,
  truncate,
  getInitials,
  formatCurrencyCustom,
  formatNumberFR,
} from './formatters';
